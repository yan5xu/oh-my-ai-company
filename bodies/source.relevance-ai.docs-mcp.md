# Relevance AI MCP Client 文档

MCP Client 支持 Notion、Canva、Atlassian 等 preset，也可连接远程 Streamable HTTP MCP server；可为同一 MCP 配多个 credentials/instances。

当前不支持 local MCP servers。MCP 扩大工具面，同时把远程 server 信任、凭据 scope、响应注入和审计带入治理边界。

# Sapiom 官网：Build agentic products

官网当前将 Sapiom 定位为 “execution engine that converts agent intent into outcomes”，产品分成 Create、Run、Act、Control、Pay 五层。

可确认能力：typed agent graph；MCP/Skill/SDK；本地测试与云端 runtime；统一接入 50+ 服务；模型、浏览器、沙盒、数据库、通信和多媒体等 capability；按调用计费；预算与治理。

公开价格为 usage-based，无订阅和最低消费。页面列出的单次示例价格包括 search `$0.006`、web extraction `$0.01`、image `$0.004/MP`、verification `$0.015`、audio `$0.08`，模型按 token。

首页同时展示 500+ agents running、100K+ transactions completed、$2M+ expenses saved、production model spend reduction 90% 等公司自报指标。页面 hydration 后计数器状态出现过归零，不作为独立规模证据。

证据边界：S1 官方产品叙事与价格快照；没有验证客户、收入、生产成功率或指标定义。

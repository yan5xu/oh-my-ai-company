# Polsia Traffic provider STOP 2026-08-02

cici-traffic 在 `default` profile 独立核验 Similarweb 与 Semrush，两个内部入口都在 report 加载前返回 `ERR_CONNECTION_CLOSED`，最终为 `chrome-error://chromewebdata/`、空 title/body/H1。没有重复 open、切 node、改账户或绕过。

因此当前无法确认 Similarweb 是否已经 closed July，也没有新 cards/raw series；Semrush 当前 scope/cards/trend 同样不可确认。

历史边界：2026-07-15 Similarweb observation 是 Jan–Jun 2026、Worldwide、All Traffic、`include_subdomains=true`，而本次目标是 root-only，因此不能冒充同口径更新；历史对象也没有保存 monthly raw series。Semrush 历史卡片只有 point-in-time 摘要，缺当前完整 controls/trend raw。

本 source 证明当前 provider 访问 STOP 与历史可比限制，不是 Polsia no-data、zero traffic 或产品状态证据。


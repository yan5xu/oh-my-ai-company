# Dust X
# Dust X

URL: https://x.com/dust4ai

候选 X 触点。`pinixc site twitter user --screen_name dust4ai` 能返回 URL，但 `list-add-member` 返回 `User not found`，因此暂标 `candidate`。需要等 Twitter list/add 或 user lookup 进一步验证后再作为稳定监控入口。

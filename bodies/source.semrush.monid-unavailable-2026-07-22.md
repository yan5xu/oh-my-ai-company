# Monid Semrush Domain Overview provider unavailable

目标是 SEO Toolkit Domain Overview / root domain `monid.ai`。内部 provider 节点返回 `code: 3` 并落到用户中心，无法确认 product、module、object、Worldwide、Desktop、report date 或 currency，因此按 STOP contract 结束。

没有切换节点、重复 open、修改账号、伪造空值或用 Similarweb 代替 Semrush。

证据边界：S4 metadata-only failure receipt；不是 no-data，也不是 Monid 在 Semrush 中为零。

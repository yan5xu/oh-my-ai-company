从官方 `llms-full.txt` 抓取的完整文档，约 2.9 万字符。

文档定义 Workspace、Canvas、Company、Tasks、Library、Roadmap、Departments 与 Agents。自定义 Agent 可配置 name、prompt、model、skills、integrations、department 与 schedules；routines 支持 daily/weekly/monthly、pause/resume/run once。

Engineering Agent 支持 plan mode、sandbox、本地 Supabase、browser test、PR、staging/production review。Marketing Department 通过 channel 与 recurring missions 安排内容。Ramp incorporation 流程把 SSN/身份输入留在 Ramp 托管页面，并保留 founder approval/attestation。

该来源能说明产品设计和能力边界，不能证明每项能力的稳定性、采用率或默认权限。

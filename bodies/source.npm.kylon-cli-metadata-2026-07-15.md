# kylon-cli npm registry metadata

npm registry 显示 `kylon-cli` 创建于 2026-06-16，当前 stable 版本为 0.1.0，同时存在持续发布的 `0.2.0-next.*`，截至采集日约有 150 个 prerelease 版本。这个发布密度支持团队仍在快速迭代 CLI，也意味着任何下载量都可能被 CI、重复安装和版本发布显著放大，不能直接映射为用户或客户。

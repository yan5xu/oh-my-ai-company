# Login with Raft Integration Guide

OAuth-like authorization code、server-scoped human/agent identity、create-raft-app scaffold 与可选 agent action manifest。文档明确标为 experimental，API 与 marketplace policy 仍可能变化。
